{
  "name": "helearn",
  "private": true,
  "scripts": {
    "build": "npm --prefix Healern.com install --include=dev && npm --prefix Healern.com run build && node -e \"const fs=require('fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.cpSync('Healern.com/dist','dist',{recursive:true});\"",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": ">=22.0.0 <23.0.0"
  },
  "dependencies": {
    "lucide-react": "^1.14.0",
    "react-hook-form": "^7.74.0",
    "react-router-dom": "^7.14.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.13",
    "tailwindcss": "^4.2.4"
  }
}
